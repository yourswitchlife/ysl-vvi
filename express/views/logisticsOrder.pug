doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title 物流訂單表單
  body
    h1 物流訂單
    form(action=actionURL method="post")
      // 在這裡添加表單元素，例如輸入框等
      button(type="submit") 提交訂單
